import TopBar from '../components/TopBar'

export default function GeneralSettings() {
  return (
    <div id="general-settings">
      <TopBar title='Settings' subtitle='General' values={false} />
    </div>
  )
}